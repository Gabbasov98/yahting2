"use strict";
"use strict";

$("._toggle-menu").click(function () {
  $(".header").toggleClass("_open");
  $("body").toggleClass("fixed-body");
});
$("._toggle-lang").click(function () {
  $(this).parents(".lang-select").toggleClass("_active");
});
$(document).mouseup(function (e) {
  var div = $(".lang-select");
  if (!div.is(e.target) && div.has(e.target).length === 0) {
    if ($(div).hasClass("_active")) {
      $(div).removeClass("_active");
    }
  }
});
"use strict";

function Sliders() {
  var SlidersArray = [];
  var sliders = document.querySelectorAll(".slider");
  sliders.forEach(function (slider, index) {
    var isLoop = slider.classList.contains("_loop");
    slider.setAttribute("data-slider-id", index);
    SlidersArray[index] = new Swiper(".slider[data-slider-id=\"".concat(index, "\"] .swiper-container"), {
      slidesPerView: "auto",
      spaceBetween: 0,
      watchSlidesProgress: true,
      preventClicks: true,
      a11y: false,
      observer: true,
      observeParents: true,
      observeSlideChildren: true,
      loop: isLoop,
      navigation: {
        nextEl: ".slider[data-slider-id=\"".concat(index, "\"] .swiper-button-next"),
        prevEl: ".slider[data-slider-id=\"".concat(index, "\"] .swiper-button-prev")
      },
      pagination: {
        el: ".slider[data-slider-id=\"".concat(index, "\"] .swiper-pagination"),
        type: 'bullets',
        clickable: true
      }
    });
  });
}
function gallery() {
  var SlidersArray = [];
  var sliders = document.querySelectorAll(".slider2");
  sliders.forEach(function (slider, index) {
    slider.setAttribute("data-slider-id", index);
    SlidersArray[index] = new Swiper(".slider2[data-slider-id=\"".concat(index, "\"] .swiper-container"), {
      slidesPerView: 1,
      spaceBetween: 0,
      navigation: {
        nextEl: ".slider2[data-slider-id=\"".concat(index, "\"] .swiper-button-next"),
        prevEl: ".slider2[data-slider-id=\"".concat(index, "\"] .swiper-button-prev")
      },
      pagination: {
        el: ".slider2[data-slider-id=\"".concat(index, "\"] .swiper-pagination"),
        type: 'fraction'
      }
    });
  });
}
Sliders();
"use strict";

$(".roadmap-card__btn").click(function () {
  $(".roadmap-item").removeClass("_active");
  $(this).parents(".roadmap-item").addClass("_active");
  $(".roadmap__grid").addClass("_open");
});
$(".roadmap-detail__btn").click(function () {
  $(".roadmap-item").removeClass("_active");
  $(".roadmap__grid").removeClass("_open");
});
"use strict";

$(".reviews").each(function (i, el) {
  reviews($(el));
});
function reviews(reviews) {
  var current = 0;
  var cardsCount = $(reviews).find(".review-card").length;
  $(reviews).find("._more-reviews").click(function () {
    current += getCurrentCount();
    if (current + getCurrentCount() >= cardsCount) {
      $(this).addClass("_hide");
      $(reviews).find("._hide-reviews").removeClass("_hide");
    }
    for (var i = current + 1; i < current + 9; i++) {
      var card = $(reviews).find(".review-card:nth-child(".concat(i, ")"));
      $(card).addClass("_active");
    }
  });
  $("._hide-reviews").click(function () {
    $(this).addClass("_hide");
    $(reviews).find("._more-reviews").removeClass("_hide");
    $(reviews).find(".review-card").removeClass("_active");
  });
}
function getCurrentCount() {
  if (window.innerWidth > 992) {
    return 8;
  }
  if (window.innerWidth < 993 && window.innerWidth > 576) {
    return 6;
  }
  return 4;
}
"use strict";
"use strict";

var fileInput = document.querySelector("[data-upload-id='file-input']");
if (fileInput) {
  console.log(fileInput);
  var upload = new FileUploadWithPreview.FileUploadWithPreview('file-input');
  upload.options.multiple = true;
  $(document).on('change', '.input-container input', function (e) {
    $(this).parents(".file-input").addClass("_uploaded");
  });
  $(document).on('click', '.image-preview-item-clear', function (e) {
    var count = $(this).parents(".file-input").find(".image-preview-item").length;
    if (count === 1) {
      $(this).parents(".file-input").removeClass("_uploaded");
    }
  });
}
"use strict";
"use strict";

window.addEventListener('DOMContentLoaded', function () {
  fix100vh();
  window.addEventListener('resize', function () {
    fix100vh();
    getScrollBarSize();
  });
  getScrollBarSize();
  $(".course-card__show").click(function () {
    $(this).parents(".course-card").toggleClass("_active");
    $(this).siblings(".course-card__hidden").slideToggle();
  });
  $(".qa-card__show").click(function () {
    $(this).parents(".qa-card").toggleClass("_active");
    $(this).siblings(".qa-card__hidden").slideToggle();
  });
  $("._show-success").click(function () {
    $(".buy").addClass("_success");
  });
  gallery();
  var body = document.querySelector("body");
  var modalTogglers = document.querySelectorAll("[data-modal]");
  var modals = document.querySelectorAll(".modal");
  modalTogglers.forEach(function (el) {
    el.onclick = function () {
      var modalId = el.getAttribute("data-modal");
      openModal(modalId);
      if (el.classList.contains("_from-gallery-modal")) {
        $("#".concat(modalId)).addClass("_back-to-gallery");
      }
    };
  });
  function closeModals() {
    modals.forEach(function (modal) {
      modal.classList.remove("_active");
      body.classList.remove("fixed-body2");
    });
    $(".buy").removeClass("_success");
  }
  function openModal(modalId) {
    closeModals();
    var modal = document.querySelector("#".concat(modalId));
    var closeButtons = modal.querySelectorAll("._close-modal");
    modal.classList.add("_active");
    body.classList.add("fixed-body2");
    closeButtons.forEach(function (btn) {
      btn.onclick = function () {
        modal.classList.remove("_active");
        body.classList.remove("fixed-body2");
        if (modal.classList.contains("_back-to-gallery")) {
          modal.classList.remove("_back-to-gallery");
          openModal("galleryModal");
        }
      };
    });
  }
});
function fix100vh() {
  var vh = window.innerHeight * 0.01;
  document.documentElement.style.setProperty('--vh', "".concat(vh, "px"));
}
$(window).on('load resize', function () {
  if ($(window).width() <= 992) {
    $('.contest-advantages').insertAfter('.contest2__grid');
  } else {
    $('.contest-advantages').insertAfter('.contest2__text2');
  }
});
var getScrollBarSize = function () {
  var el = window.document.createElement('textarea'),
    style = {
      'visibility': 'hidden',
      'position': 'absolute',
      'zIndex': '-2147483647',
      'top': '-1000px',
      'left': '-1000px',
      'width': '1000px',
      'height': '1000px',
      'overflow': 'scroll',
      'margin': '0',
      'border': '0',
      'padding': '0'
    },
    support = el.clientWidth !== undefined && el.offsetWidth !== undefined;
  for (var key in style) {
    if (style.hasOwnProperty(key)) {
      el.style[key] = style[key];
    }
  }
  return function () {
    var size = null;
    if (support && window.document.body) {
      window.document.body.appendChild(el);
      size = [el.offsetWidth - el.clientWidth, el.offsetHeight - el.clientHeight];
      window.document.body.removeChild(el);
    }
    document.documentElement.style.setProperty('--scrollWidth', "".concat(size[0], "px"));
    return size;
  };
}();
$("._more-course-detail").click(function () {
  $(".course-detail").addClass("_show-all");
});
$("._hide-course-detail").click(function () {
  $(".course-detail").removeClass("_show-all");
});
//# sourceMappingURL=../sourcemaps/main.min.js.map
